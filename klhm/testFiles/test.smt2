(foo ()())

(    	baz)

(push 1)

(define-fun name () Bool (( )) )

(zack)

(define-funs-rec ((name () Bool) (name2 () Bool))
                  ((voll koffer) (nass ass))
)

(check-sat)
(push 1)

(define-fun name () Bool (( )) )
(define-fun-rec name () Bool (( )) )
(lalal)

(declare-datatypes ( ( Tree 1) ( TreeList 1) ) (
; Tree
( par ( X ) ( ( node ( value X ) ( children ( TreeList X )) )))
; TreeList
( par ( Y ) ( ( empty )
( insert ( head ( Tree Y )) ( tail ( TreeList Y ))) ))))

(check-sat)
(pop 2)

(declare-datatype IntList
( ( empty )
( insert ( head Int ) ( tail IntList ) )))

(check-sat)
(bar)
